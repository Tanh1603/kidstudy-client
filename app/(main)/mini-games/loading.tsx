/* eslint-disable import/order */
import Loading from '@/components/loading';
import React from 'react';

const LoadingMiniGames = () => {
    return (
        <Loading/>
    );
}

export default LoadingMiniGames;
